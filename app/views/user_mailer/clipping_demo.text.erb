Foodie — Gmail Clipping Demo

This message repeats its content <%= @block_count %> times so the HTML part
crosses Gmail's ~102KB clipping threshold. Open the HTML version in Gmail to
see the "[Message clipped] View entire message" behavior.

Log in: <%= @url %>
